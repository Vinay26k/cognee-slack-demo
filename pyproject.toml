[project]
name = "cognee-demo-slack"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.122.0",
    "certifi>=2026.7.22",
    "cognee[fastembed]",
    "fastapi>=0.141.1",
    "python-dotenv>=1.2.2",
    "python-multipart>=0.0.32",
    "uvicorn[standard]>=0.52.3",
]

[tool.uv.sources]
cognee = { git = "https://github.com/topoteretes/cognee.git", rev = "dev" }
